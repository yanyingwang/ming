#lang racket/base

(provide mapping)

(define (mapping)
  '((lambda 道) ;; 入
    (define 名) ;; 旨 意 义
    (let 令)

    (require 招) ;; 召见 , 引入      入 出 拿 给 引进 输出 j举,承 受
    (provide 授) ;; 把....供起来   奏 奉(侍奉,奉献,供奉) 贡 献 供 授


    ;;;;;; conditaionals
    ;; 如 若 倘 况
    (if 若) ;; 若是
    (case 倘) ;; 倘如
    (cond 当) ;; 在……时候
    (else 否则)

    (and 且) ;; 也 还 又 且
    (or 或) ;; 不
    ))
